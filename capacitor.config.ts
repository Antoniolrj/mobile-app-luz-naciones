import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'iglesialuznaciones.es.app',
  appName: 'app-luz-naciones',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
